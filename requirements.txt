# Runtime deps do dashboard Streamlit.
# Slim — sem [dev] e sem [api] do pyproject.toml.
# Para o setup completo (dev/lint/test), use: pip install -e ".[dev]"

# Torch CPU-only — wheel ~200MB vs ~600MB do default com CUDA.
# Streamlit Cloud não tem GPU, então CPU wheel é mais rápido pra instalar.
--extra-index-url https://download.pytorch.org/whl/cpu

numpy>=1.26
pandas>=2.2
scipy>=1.13
scikit-learn>=1.5
torch>=2.2
shap>=0.46
matplotlib>=3.9
streamlit>=1.36
plotly>=5.22
pyarrow>=16
tqdm>=4.66
joblib>=1.4
